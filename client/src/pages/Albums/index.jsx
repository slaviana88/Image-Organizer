import React from 'react';
import {Link} from 'react-router-dom';

import paths from 'paths';

class Albums extends React.Component {
  render() {
    return <div>All albums list here</div>;
  }
}

export default Albums;
